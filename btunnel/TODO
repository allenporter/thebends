short term:
- separate client + server and forwarding service records to remote hosts.

longer term:
- libnatpmp / chownnat library integration (samy?)
- GUI
- remote server discovery (generic web based api?)
